# Adapt Requirements to Standard Template

Use the `requirements` skill (Workflow C) to migrate an existing requirements document into the standard FR/NFR/TR
template format.

## Arguments

`$ARGUMENTS` — Path to the requirements file to adapt.

**Examples:**

- `docs/old-requirements.txt`
- `prd.md`
- `requirements/auth.md`
- `SPEC.md`
